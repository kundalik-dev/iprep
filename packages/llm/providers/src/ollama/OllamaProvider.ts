// ILLMProvider — local Ollama REST API on :11434
// isAvailable(): GET http://localhost:11434/api/tags to check if Ollama is running
// analyze(): POST /api/generate with transcript + prompt, streams response
export {};
