// ProviderRegistry — resolveAnalysisProvider(), resolveSTT(), resolveTTS(), resolveAgent()
// Fallback chain: GeminiFree → GeminiAPI → ClaudeCLI → GeminiCLI → CodexCLI → Ollama → ClaudeAPI → OpenAI
export {};
