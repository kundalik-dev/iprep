// IAgentProvider — Deepgram Voice Agent all-in-one: STT + LLM + TTS via single WebSocket
// isAvailable(): checks DEEPGRAM_API_KEY is configured
// createSession(): returns AgentSession connected to Deepgram agent API
export {};
