// ILLMProvider — Anthropic API via @anthropic-ai/sdk (BYOK)
// isAvailable(): checks ANTHROPIC_API_KEY is configured
// complete(): streams text via Messages API
// analyze(): sends transcript + analysis prompt, returns AnalysisResult
export {};
