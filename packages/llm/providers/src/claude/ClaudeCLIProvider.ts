// ILLMProvider — spawns local Claude CLI: claude --output-format stream-json "..."
// isAvailable(): which('claude') check
// analyze(): spawns claude process, parses stream-json output, returns AnalysisResult
export {};
